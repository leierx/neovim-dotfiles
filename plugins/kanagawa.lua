return {
  "rebelot/kanagawa.nvim",
  -- opts = {
  -- overrides = function(colors) return { LineNr = { fg = colors.theme.ui.fg_dim } } end, -- slightly brighter line numbers
  -- },
  config = function() vim.cmd("colorscheme kanagawa") end,
}

